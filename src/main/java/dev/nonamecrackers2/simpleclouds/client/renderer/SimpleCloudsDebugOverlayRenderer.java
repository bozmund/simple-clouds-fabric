package dev.nonamecrackers2.simpleclouds.client.renderer;

import net.minecraft.client.gui.GuiGraphicsExtractor;
import net.minecraftforge.client.gui.overlay.ForgeGui;

/**
 * 26.2 vertical-slice stub. The debug overlay (rendered in debug mode) is not ported yet;
 * this is a no-op that keeps the API. Port incrementally.
 */
public class SimpleCloudsDebugOverlayRenderer
{
	public static void render(ForgeGui gui, GuiGraphicsExtractor stack, float partialTicks, float width, float height)
	{
		// TODO(26.2): port the debug overlay.
	}
}
