package dev.nonamecrackers2.simpleclouds.common.init;

/**
 * 26.2 vertical-slice stub. Custom Brigadier command argument registration
 * (ArgumentTypeInfos.registerByClass) was removed from the new backend; the cloud type
 * command argument is deferred. Port incrementally.
 */
public class SimpleCloudsCommandArguments
{
	public static void register(net.minecraftforge.eventbus.api.IEventBus modBus)
	{
		// TODO(26.2): register the cloud type command argument via the new API.
	}
}
