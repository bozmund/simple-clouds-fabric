org.gradle.jvmargs=-Xmx3G
org.gradle.parallel=true
org.gradle.configuration-cache=false

# Fabric Properties — same as the proven template / running profile
minecraft_version=26.2
loader_version=0.19.5
loom_version=1.17.20

# Mod properties
mod_version=0.7.3+26.2-fabric
maven_group=dev.nonamecrackers2.simpleclouds
archives_base_name=simple-clouds

# Dependencies — same Fabric API build as installed in the profile
fabric_version=0.160.0+26.2
